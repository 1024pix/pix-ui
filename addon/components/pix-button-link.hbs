{{#if @route}}
  {{#if @model}}
    <LinkTo
      @route={{@route}}
      @model={{@model}}
      @disabled={{@isDisabled}}
      @query={{@query}}
      class={{this.className}}
      ...attributes
    >
      t {{yield}}
    </LinkTo>
  {{else}}
    <LinkTo
      @route={{@route}}
      @disabled={{@isDisabled}}
      @query={{@query}}
      class={{this.className}}
      ...attributes
    >
      {{yield}}
    </LinkTo>
  {{/if}}
{{else}}
  <a href={{@href}} class={{this.className}} ...attributes>
    {{yield}}
  </a>
{{/if}}
