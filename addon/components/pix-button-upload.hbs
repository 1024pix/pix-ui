<label for={{@id}} class={{this.className}}>
  {{yield}}
</label>
<input
  role="button"
  id={{@id}}
  type="file"
  class="pix-button-upload__input"
  value={{this.files}}
  {{on "change" this.handleChange}}
  ...attributes
/>
