<label for={{@id}} class={{this.className}} role="button">
  {{yield}}
</label>
<input
  id={{@id}}
  type="file"
  class="screen-reader-only"
  value={{this.files}}
  {{on "change" this.handleChange}}
  ...attributes
/>